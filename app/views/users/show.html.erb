<div class="user-show-page">

  <%= render "partial/navbar" %>

  <div class="profile mt-4">
    <% if @user.profile_image.url.nil? %>
      <div class="profile-default-img"></div>
    <% else %>
      <%= image_tag @user.profile_image.url, class: "profile-img" %>
    <% end %>

    <div class="profile-name mt-4">
      <%= @user.nickname %> <%= @age %>歳 <%= @user.residence%>
    </div><br>
    
    <% if @user.movie_information.present? %>
      <div class="prafile-text">
        <p>◎観たい映画</p>
        <%= @user.movie_information%>
      </div><br>
    <% end %>

    <% if @user.self_introduction.present? %>
      <div class="prafile-text">
        <p>◎自己紹介</p><%= safe_join(@user.self_introduction.split("\n"),tag(:br))%>
      </div>
    <% end %>

  </div>

  <% if current_user.id == @user.id %>
    <div class="user-action">
      <div class="user-action-common">
        <%= link_to destroy_user_session_path, method: :delete do %>
          <i class="fas fa-sign-out-alt fa-2x"></i>
        <% end %>
        <span>ログアウト</span>
      </div>
      <div class="user-action-common">
        <%= link_to edit_user_registration_path do %>
          <i class="fas fa-pencil-alt fa-2x"></i>
        <% end %>
        <span>情報を編集<span>
      </div>
    </div>
  <% end %>


</div>